# TODO - Floating props parity across pages

- [x] Assets confirmed: circuit_icon.svg / connection_icon.svg / cube_icon.svg
- [x] Add the floating decorative props system to `projects.py` similar to `main-page.py`.

- [ ] Add the same floating props system to `qualifications.py`.
- [ ] Add the same floating props system to `blog.py`.
- [ ] Ensure props start hidden and animate (opacity/scale) on reveal.
- [ ] Ensure props drift + rotate in each page’s existing `animate(tick)` loop.
- [ ] Commit changes.
- [ ] Push to GitHub.

